// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        inputBorderYellow: "#E8CB2B",
        borderGreen: "#035C08F5",
        white: "#FFFFFF",
        red: "#FF0000"
    },
});

export default theme;
